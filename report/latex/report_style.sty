\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{report_style}[2026/07/24 Simulation engineering final report style]

\RequirePackage[dvipdfmx]{graphicx}
\RequirePackage{amsmath,amssymb}
\RequirePackage{listings}
\RequirePackage{bm}
\RequirePackage{url}
\RequirePackage{subfigure}
\RequirePackage{verbatim}
\RequirePackage{wrapfig}
\RequirePackage{ascmac}
\RequirePackage[top=35truemm,bottom=30truemm,left=30truemm,right=30truemm]{geometry}
\RequirePackage{fancyhdr}
\RequirePackage{booktabs}
\RequirePackage{array}
\RequirePackage{calc}
\RequirePackage{makecell}

\pagestyle{fancy}
\lhead{2026年度 シミュレーション工学・演習 最終課題}
\rhead{}
\cfoot{\thepage}
\renewcommand{\headrulewidth}{0pt}
\setlength{\headheight}{16pt}

\makeatletter
\long\def\@makecaption#1#2{%
  \vskip\abovecaptionskip
  \iftdir\sbox\@tempboxa{#1\hskip1zw#2}%
  \else\sbox\@tempboxa{#1\hskip1zw#2}%
  \fi
  \ifdim \wd\@tempboxa >\hsize
    \iftdir #1\hskip1zw#2\relax\par
    \else #1 #2\relax\par\fi
  \else
    \global\@minipagefalse
    \hbox to\hsize{\hfil\box\@tempboxa\hfil}%
  \fi
  \vskip\belowcaptionskip}
\makeatother

\lstset{
  frame=single,
  numbers=left,
  numbersep=6pt,
  tabsize=2,
  breaklines=true,
  breakatwhitespace=false,
  basicstyle=\ttfamily\small,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false
}

% upLaTeXの和欧文間隔を文書全体の規則として使用し、個別の\hspace補修を避ける。
\AtBeginDocument{\autospacing\autoxspacing}
\providecommand{\tightlist}{}
\providecommand{\passthrough}[1]{#1}
\newcommand{\inlinecode}[1]{\texttt{#1}}
\newcommand{\englishterm}[1]{\textit{#1}}
